## Overview

This project implements a binary classification perceptron trained from scratch using NumPy, paired with a concurrent multi-worker load generator that simulates real-time inference traffic, injects configurable anomalies, and logs all activity to a JSONL file.

## Project Structure

```txt
simple-perceptron-load-simulator/
├── main.ipynb
├── workers_<timestamp>.jsonl
└── README.md
```

## Model

The `SimplePerceptron` implements the classic single-layer perceptron with:

- Net input: `z = w · x + b`
- Step function: `1 if z >= 0, else 0`
- Learning rule: `w += lr * (y - ŷ) * x` and `b += lr * (y - ŷ)`
- Early stopping: halts training after N epochs without improvement
- Uncertainty head: cosine similarity and angle between input vector and weight vector

### Training Hyperparameters

| Parameter | Value |
|---|---|
| Learning rate | 0.001 |
| Max epochs | 1028 |
| Early stopping threshold | 37 |
| Seed | 137 |
| Vector dimensions | 11 |

## Load Generator

The `LoadGenerator` launches N concurrent workers using `multiprocessing`. Each worker:

1. Uses an independent PCG random number generator seeded sequentially from a base seed.
2. Samples either a normal vector or an anomaly based on the configured ratio.
3. Runs inference through the trained model.
4. Computes uncertainty metrics via `uncertainty_head`.
5. Sleeps 1ms and writes the result to a shared JSONL log protected by a `Lock`.

### Anomaly Generation

Anomalies are generated by scaling a uniform random vector by a factor outside the normal range, either positive `[1, 2.35]` or negative `[-3, -1]`, and are labeled with `real_value = -1`.

### Log Format

Each record written to the JSONL file contains:

```json
{
  "timestamp": "2026-03-04T05:14:05.003058+00:00",
  "vector": [0.51, 0.18, ...],
  "real_value": 1,
  "model_output": 1,
  "vectors_angle": 33.36,
  "cosine_similarity": 0.8353
}
```

The first record in each session contains the model weights and bias used for that run.

## Performance (Session 20260304T051404)

*Session performance truncated due to the upload limit on GitHub, real testing had a duration of 3 minutes.*

| Metric | Value |
|---|---|
| Total records | 92,999 |
| Duration | 18.3 seconds |
| Throughput | ~5,082 records/sec |
| Workers | 108 |
| Anomaly ratio observed | 13.03% |
| Model accuracy (normal samples) | 79.22% |
| Recall | 99.88% |
| False positive rate | 98.58% |

> The high false positive rate indicates a labeling mismatch between the training generator and the load generator's `_sample()` function.

## Requirements

```
numpy
```
